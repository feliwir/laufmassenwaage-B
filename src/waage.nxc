#include "NXCDefs.h"
#define MOVE_SPEED	40

task main ()
{
	OnFwd(OUT_A, MOVE_SPEED);
	SetSensorColorFull(IN_1);
	bool done = false;

	while(!done)
	{
		while(SENSOR_1 == 2) //blau
		{
			done = true;
		}
	}

	Off(OUT_A);
}
